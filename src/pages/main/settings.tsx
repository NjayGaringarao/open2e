export default function Settings() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-[var(--textHeader)] mb-4">
        Settings
      </h2>
      <p>Change app preferences or theme options here.</p>
    </div>
  );
}
